package com.openrouter.gateway.usage;

/**
 * Retained for compilation only — all callers removed.
 * Usage limit enforcement removed: each user uses their own OpenRouter API key (BYOK).
 * OpenRouter enforces per-key rate limits upstream.
 */
@Deprecated
@org.springframework.stereotype.Service
public class UsageTrackingService {
}
