package com.openrouter.gateway.usage;

/**
 * Retained for compilation only — endpoints removed.
 * Usage tracking removed: each user uses their own OpenRouter API key (BYOK).
 */
@Deprecated
public class UsageController {
}
